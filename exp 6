clear;
clc;

// Dimensions (m)
a = 5 * 10^-2;
b = 4 * 10^-2;
c = 10 * 10^-2;

// Conductivity (S/m)
C = 5.8 * 10^7;

// Permeability of free space (H/m)
Uo = 4 * %pi * 10^-7;

// Resonant frequency (Hz)
f101 = 3.335 * 10^9;

// Skin depth (m)
d = sqrt(1 / (%pi * f101 * Uo * C));

// Quality factor of TE101 mode
Q = ((a^2 + c^2) * a * b * c) / ...
    (d * (2 * b * (a^3 + c^3) + a * c * (a^2 + c^2)));

disp(Q, "Quality factor of TE101 = ");
